public class K1ObjetosIntro
{
    public static void main(String[] args) {

        //Operador new reserva memoria estática para un objeto
        /*
        prog estructurada solo se decriben funciones a las cuales se les pasan datos a dif de la prog orientadaa
        objetos donde trabajamos con entidades.
        características=datos y métodos=comportammiento o funcionalidades

        Encapsulamiento es que no se sabe la implementación, solo lo que hace; puede haber dos implementaciones
        distintas de un mísmo método pero nosotros no lo vemos, solo llamamos al método

        Ocultacion hace referencia a no ver los atributos (siempre private) y solo se pueda acceder a ellos solo
         por métodos publicos para leer y editarlos etc.. Ocultar ele stado o los atributos de la clase

        Polimorfismo diferentes formas de hacer lo mismo (sobrecarga de métodos y sobrescritura)

        alt+insert= generador de geter-seter constructor y otros métodos etc

        Orden de class: Atributos, constructor, geters-seters, métodos propios, métodos sobrescritos(Override)

        .equals al heredar de objeect todos lo tienen pero compara con ==; Lo que se suele hacer es sobrescribir el
        método para devolver cuando coinsideramos que dos objetos son igualesç

        .toString devuelve hashcode por defecto heredado de object, se modifica

        Static es de la clase, el valor estático esta compartido por todas las instancias de la clase, para el caso
        de los métodos, no es necesario una instancia para llamarlo

        Final constante (van en mayúsculas) o en clase para no heredar o en métodos para nó sobrescribir

        Enum o enumeradores contieen coleccion de valores constantes, similar a clases sin seters
        Mejor explicado en K2

        public enum Color{
        ROJO("Rojo"),
        AZUL("Azul"),
        VERDE("Verde");

        private final String color

        public Color(String color){this.color=color}

        public String getColor(){ return color}

        }

        Color.ROJO //crea un enum con valores que estan dentro de ()

        Método .addAlgo para añadir objetos a un arreglo de objetos el ejemplo se da en k2

        .compareTo para comparar se implementa comparable



         */


    }
}
